# BurrowWeb

Web app for testing of Wabbit bot. NOT the companion app, but strictly for testing/development.

## Running App (Dev)

## Running App (Production)

Make sure that there is a recent build folder

`npm run build`

Then, serve

`npx serve -s dist `
